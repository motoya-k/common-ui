import React from "react";

type BaseProps = {};
type Props = BaseProps;
const Alert: React.FC<Props> = (props) => {
  return (
    <div>
      <h1>Alert</h1>
    </div>
  );
};

export default Alert;
